extends: baseline.yaml

name: proxy_p1_fast
description: "Five-minute 1xH100 baseline control for Phase 0 calibration"

env:
  MAX_WALLCLOCK_SECONDS: "300"
  VAL_LOSS_EVERY: "0"
  TRAIN_LOG_EVERY: "25"
  WARMUP_STEPS: "1"
